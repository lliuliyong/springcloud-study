server:
  port: 8881
management:
  endpoints:
    web:
      exposure:
        include: "*"
spring:
  application:
    name: hello-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
